'use client';
import SearchPageContainer from '@/features/search/containers/SearchPageContainer';

export default function PlaylistsPage() {
  return <SearchPageContainer tabOverride="playlists" />;
}